# 一劳永逸部署指南：手机也能用这套 Skill

## 这套东西解决什么

| 环节 | 谁来做 | 说明 |
|---|---|---|
| 出教案 / 词表 / 句型 | **手机智能体**（豆包、元宝、Kimi…） | 用「手机智能体系统提示词」喂进去即可 |
| 把词表变成 PDF + 点读页 | **云端 GitHub Actions** | 手机跑不了脚本，交给云端 |
| 打开使用 | **手机浏览器** | GitHub Pages 链接，点开即用、断网可听 |

一句话闭环：**手机上让智能体出词表 → 放进仓库 → 云端自动生成 → 手机打开链接用。**

---

## 第一步：把文件放进你的仓库

用 GitHub 网页上传（注意目录别放错）：

| 本地文件 | 仓库中的路径 |
|---|---|
| `.github/workflows/build.yml` | `.github/workflows/build.yml` |
| `themes/mid-autumn/*` | `themes/mid-autumn/`（4 个 tsv + title.txt） |
| `themes/weather/*` | `themes/weather/` |
| `scripts/build_site.py` | `scripts/build_site.py` |
| `scripts/gen_*.py`（4 个） | `scripts/`（覆盖旧的） |

> `scripts/gen_printables.py` 与 `gen_bingo.py` 必须是**修复版**（含线稿涂色卡 + 跨平台字体），不确定就全部覆盖一遍。

---

## 第二步：开启 GitHub Pages

```text
仓库页面 → Settings → Pages（左侧）
Source: Deploy from a branch
Branch: main    Folder: /docs
点 Save
```

---

## 第三步：等构建完，手机打开

1. 上传完会自动触发 Actions（约 2–4 分钟），在仓库 **Actions** 标签页可看进度（绿勾=成功）
2. 打开 **`https://mint9976.github.io/kids-english-coach/`**
3. 手机浏览器里：点主题 → 点「🔊 单词点读」→ 点词发音（**断网也能响**）
4. PDF 教具：手机可预览，**打印请在电脑上打开**

把这个网址**生成二维码**贴在家里，家长扫码即用，连文件都不用传。

---

## 日常怎么用（三种场景）

**① 加一个新主题（比如"动物"）**
```text
1. 手机让智能体生成 words.tsv / sentences.tsv（它会按格式输出）
2. GitHub 网页 → themes/ → Add file → Create new file
   路径填：themes/animals/words.tsv     ← 目录名用英文
   粘贴内容 → Commit
3. 可选：再建 themes/animals/title.txt，内容写「动物」
4. 等 3 分钟 → 刷新 Pages，新主题出现
```

**② 改词 / 加词**
```text
直接编辑 themes/xxx/words.tsv → Commit → 自动重建
```

**③ 手动立刻重建**
```text
仓库 → Actions → Build Kids English Materials → Run workflow
```

---

## 出问题怎么查

| 现象 | 原因 | 解决 |
|---|---|---|
| Actions 红叉 | 缺字体或依赖 | 看日志；确认 `build.yml` 里装了 `fonts-noto-cjk` |
| 日志报「中文字体缺失」 | runner 没装 CJK 字体 | 装 `fonts-noto-cjk` 或 `fonts-wqy-zenhei` |
| Pages 打开 404 | 没选 `/docs` 或 Actions 没跑完 | Settings→Pages 检查 Folder=`/docs`；等构建完成 |
| 点读页没声音 | 手机静音 / 微信内置浏览器 | 关静音键；用 Chrome / Safari 打开，别用微信内置浏览器 |
| 主题没出现 | 目录名有中文或少了 words.tsv | `themes/<英文名>/words.tsv` 必须有 |

---

## 目录结构最终样子

```
kids-english-coach/            ← 仓库根目录
├── .github/workflows/build.yml
├── themes/
│   ├── mid-autumn/   words.tsv sentences.tsv caller.tsv caller_sentences.tsv title.txt
│   ├── weather/      （同上）
│   └── animals/      ← 你以后加的
├── scripts/          gen_*.py + build_site.py
├── docs/             ← Actions 自动生成，别手改
├── SKILL.md  README.md  install.sh
└── references/  assets/  samples/
```
